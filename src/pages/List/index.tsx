import CardGrid from '@components/CardGrid';
import MenuTemplate from '@templates/menu';

function List() {
  return (
    <MenuTemplate>
      <CardGrid />
    </MenuTemplate>
  );
}

export default List;
